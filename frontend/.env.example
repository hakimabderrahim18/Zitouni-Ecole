# Base URL of the deployed backend API (Render).
# Leave EMPTY for local development (Vite proxy handles /api and /socket.io).
# Example for production:
# VITE_API_URL=https://zitouni-ecole-backend.onrender.com
VITE_API_URL=
