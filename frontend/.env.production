# Empty in production: the frontend uses relative /api and /socket.io paths,
# which Vercel proxies to the Render backend via vercel.json rewrites.
# This keeps requests same-origin (no CORS) and requires no dashboard config.
VITE_API_URL=
